
{% extends 'back&front/back.html.twig' %}

{% block title %}Edit Evenement{% endblock %}

{% block body %}
<div class="container">
    <h1 class="my-4">Edit Evenement</h1>

    <div class="card">
        <div class="card-body">
            {{ include('evenement/_form.html.twig', {'button_label': 'Update'}) }}
        </div>
    </div>

    <div class="mt-4">
        <a href="{{ path('app_evenement_index') }}" class="btn btn-secondary me-2">Back to List</a>
        {{ include('evenement/_delete_form.html.twig') }}
    </div>
</div>
{% endblock %}

