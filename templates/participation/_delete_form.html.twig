<form method="post" action="{{ path('app_participation_delete', {'idP': participation.idP}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ participation.idP) }}">
    <button class="btn">Delete</button>
</form>
